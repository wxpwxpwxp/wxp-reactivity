

describe('arrayLast Function', ()=>{
  it('numbers get last by arrayLast', () => {
    // expect(isObject(objectTokenizations)).toBe(true);
    expect(3).toBe(3);
    expect(null).toBe(null);
  });
});
